#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "options.h"

void getNumberOption(int argc, char** argv) {
	if (argc > 3)
	{
		int length = strlen(argv[1]), i = 0;

	}
	else
	{
		// No options
		// To simple comparaison
	}
}